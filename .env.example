# ResumeAI Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# Gmail Configuration (Required for email parsing)
# =============================================================================
# Your Gmail address for accessing LinkedIn job emails
GMAIL_ADDRESS=your.email@gmail.com

# Gmail App Password (NOT your regular Gmail password)
# Generate at: https://myaccount.google.com/apppasswords
GMAIL_APP_PASSWORD=your_16_character_app_password

# =============================================================================
# AI/LLM Configuration (Required for resume/cover letter generation)
# =============================================================================
# LLM Provider: "openai" or "anthropic"
LLM_MODEL_PROVIDER=openai

# API Key for your chosen provider
# OpenAI: Get from https://platform.openai.com/api-keys
# Anthropic: Get from https://console.anthropic.com/
LLM_API_KEY=your_api_key_here

# Model name to use
# OpenAI examples: gpt-4, gpt-4-turbo, gpt-3.5-turbo
# Anthropic examples: claude-3-opus-20240229, claude-3-sonnet-20240229
LLM_MODEL=gpt-4

# =============================================================================
# Setup Instructions
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Never commit .env to version control (it's in .gitignore)
#
# Required for basic functionality:
# - GMAIL_ADDRESS and GMAIL_APP_PASSWORD (for parsing LinkedIn job alert emails)
# - LLM_MODEL_PROVIDER, LLM_API_KEY, and LLM_MODEL (for AI-generated resumes/cover letters)
#
# How it works:
# 1. System reads LinkedIn job alert emails from your Gmail
# 2. Parses job details from email HTML
# 3. Uses AI to generate custom resumes and cover letters for each job